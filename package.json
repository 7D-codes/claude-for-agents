{
  "name": "claude-for-hermes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An MCP server that lets Hermes delegate coding work to Claude Code.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
