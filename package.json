{
  "name": "claude-for-agents",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A local MCP server that lets compatible agents delegate work to Claude Code.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
